# Local development only. Copy to .env and fill in; .env is git-ignored.
# For real use, put these into your MCP client config (see README.md) instead.
ACINFINITY_EMAIL=you@example.com
ACINFINITY_PASSWORD=your-app-password
# Optional: DEBUG, INFO (default), WARNING, ERROR
ACINFINITY_LOG_LEVEL=INFO
